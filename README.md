# conductor_server
This is a package for PHP-CLI that hosts a server for handing out jobs for other computers to complete, see https://www.tomgriffiths.net/php-cli/package-docs/conductor_server.html for function defenitions.

# Settings
- **jobsFile**: Specifies where to store the jobs information, expects string, default of "conductor\\jobs.json".